name: Deploy to VPS

on:
  push:
    branches:
      - main
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and pull
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          envs: BRANCH_NAME,PROD_EXTENSION_BASE_URL,STAGING_EXTENSION_BASE_URL,PROD_EXTENSION_PEM_PATH,STAGING_EXTENSION_PEM_PATH
          script: |
            set -e

            migrate_existing_db() {
              DB_DIR="$1"
              mkdir -p "$DB_DIR"

              if [ ! -e "$DB_DIR/umrah.db" ] && [ -e "umrah.db" ]; then
                for file in umrah.db umrah.db-wal umrah.db-shm; do
                  if [ -e "$file" ]; then
                    mv "$file" "$DB_DIR/$file"
                  fi
                done
              fi
            }

            deploy_app() {
              APP_DIR="$1"
              BRANCH="$2"
              APP_NAME="$3"
              APP_DB_PATH="$4"
              EXTENSION_ENV="$5"
              EXTENSION_BASE_URL="$6"
              EXTENSION_PEM_PATH="$7"
              APP_DB_DIR="$(dirname "$APP_DB_PATH")"

              cd "$APP_DIR"
              pm2 stop "$APP_NAME" || true
              migrate_existing_db "$APP_DB_DIR"
              git fetch origin "$BRANCH"
              git reset --hard "origin/$BRANCH"
              rm -rf dist/
              rm -rf node_modules
              npm ci
              npm run build
              node scripts/package-extension.mjs --env "$EXTENSION_ENV" --base-url "$EXTENSION_BASE_URL" --pem "$EXTENSION_PEM_PATH" --out "$APP_DIR/public/extensions/$EXTENSION_ENV"
              DB_PATH="$APP_DB_PATH" EXTENSION_CHANNEL="$EXTENSION_ENV" pm2 restart "$APP_NAME" --update-env || DB_PATH="$APP_DB_PATH" EXTENSION_CHANNEL="$EXTENSION_ENV" pm2 start npm --name "$APP_NAME" -- start
            }

            install_backup_cron() {
              NODE_BIN="$(command -v node)"
              cat >/etc/cron.d/umrah-db-backup <<EOF
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/bin
0 * * * * root cd /var/www/umrah-prod && ${NODE_BIN} scripts/db-backup.mjs backup --db /var/lib/umrah/prod/umrah.db --dir /var/backups/umrah/prod --keep 168 >> /var/log/umrah-db-backup.log 2>&1
0 * * * * root cd /var/www/umrah-staging && ${NODE_BIN} scripts/db-backup.mjs backup --db /var/lib/umrah/staging/umrah.db --dir /var/backups/umrah/staging --keep 168 >> /var/log/umrah-db-backup.log 2>&1
EOF
            }

            if [ "$BRANCH_NAME" = "main" ]; then
              NODE_BIN="$(command -v node)"
              deploy_app /var/www/umrah-prod main umrah-prod /var/lib/umrah/prod/umrah.db prod "$PROD_EXTENSION_BASE_URL" "$PROD_EXTENSION_PEM_PATH"
              install_backup_cron
              cd /var/www/umrah-prod
              "$NODE_BIN" scripts/db-backup.mjs backup --db /var/lib/umrah/prod/umrah.db --dir /var/backups/umrah/prod --keep 168
            else
              NODE_BIN="$(command -v node)"
              deploy_app /var/www/umrah-staging staging umrah-staging /var/lib/umrah/staging/umrah.db staging "$STAGING_EXTENSION_BASE_URL" "$STAGING_EXTENSION_PEM_PATH"
              install_backup_cron
              cd /var/www/umrah-staging
              "$NODE_BIN" scripts/db-backup.mjs backup --db /var/lib/umrah/staging/umrah.db --dir /var/backups/umrah/staging --keep 168
            fi
        env:
          BRANCH_NAME: ${{ github.ref_name }}
          PROD_EXTENSION_BASE_URL: ${{ secrets.PROD_EXTENSION_BASE_URL || 'http://157.173.122.139:3000' }}
          STAGING_EXTENSION_BASE_URL: ${{ secrets.STAGING_EXTENSION_BASE_URL || 'http://157.173.122.139:3001' }}
          PROD_EXTENSION_PEM_PATH: ${{ secrets.PROD_EXTENSION_PEM_PATH || '/var/lib/umrah/prod/keys/umrah-extension.pem' }}
          STAGING_EXTENSION_PEM_PATH: ${{ secrets.STAGING_EXTENSION_PEM_PATH || '/var/lib/umrah/staging/keys/umrah-extension.pem' }}
